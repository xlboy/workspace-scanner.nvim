test:
	nvim --headless -u tests/init.lua -c "PlenaryBustedDirectory tests/${LP} { minimal_init = 'tests/init.lua' }"

# test-scanner:
	# nvim --headless -u tests/init.lua -c "PlenaryBustedDirectory tests/${LP} { minimal_init = 'tests/init.lua' }"

